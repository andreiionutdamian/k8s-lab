apiVersion: v1
kind: Service
metadata:
  name: {{ include "full-chart.fullname" . }}
  labels:
    {{- include "full-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "full-chart.selectorLabels" . | nindent 4 }}
